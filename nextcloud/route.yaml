apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: nextcloud
  name: nextcloud
spec:
  port:
    targetPort: 80-tcp
  to:
    kind: Service
    name: nextcloud
    weight: 100
